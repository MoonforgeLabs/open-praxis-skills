# Package Management Pattern

Source: affaan-m/ECC ecc-tools.json

## Manifest Structure

profiles (full/minimal/custom) + dependencyGraph + resolutionOrder + packageFiles + managedFiles + blockedRemovalComponents

## Delta Updates: requested → selected (with transitive deps)

## Multi-Harness Adapters

Same content → different target dirs (claudeCode, codex, cursor, opencode). managedFiles tracks ownership.
